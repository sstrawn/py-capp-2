name: Trigger auto deployment for containerapp upgh33

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ app33 ]
    paths:
    - '**'
    - '.github/workflows/upgh33-AutoDeployTrigger-95411bd2-23a6-4cbb-8d45-9ea9479cb4a8.yml'

  # Allow mannually trigger 
  workflow_dispatch:      

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1

      - name: Log in to container registry
        uses: docker/login-action@v1
        with:
          registry: caeuap1342174440acr.azurecr.io
          username: ${{ secrets.UPGH33_REGISTRY_USERNAME }}
          password: ${{ secrets.UPGH33_REGISTRY_PASSWORD }}

      - name: Build and push container image to registry
        uses: docker/build-push-action@v2
        with:
          push: true
          tags: caeuap1342174440acr.azurecr.io/upgh33:${{ github.sha }}
          file: ./Dockerfile
          context: ./


  deploy:
    runs-on: ubuntu-latest
    needs: build
    
    steps:
      - name: Azure Login
        uses: azure/login@v1
        with:
          creds: ${{ secrets.UPGH33_AZURE_CREDENTIALS }}


      - name: Deploy to containerapp
        uses: azure/CLI@v1
        with:
          inlineScript: |
            echo "Installing containerapp extension"
            az config set extension.use_dynamic_install=yes_without_prompt
            echo "Starting Deploying"
            az containerapp registry set -n upgh33 -g sca --server caeuap1342174440acr.azurecr.io --username  ${{ secrets.UPGH33_REGISTRY_USERNAME }} --password ${{ secrets.UPGH33_REGISTRY_PASSWORD }}
            az containerapp update -n upgh33 -g sca --image caeuap1342174440acr.azurecr.io/upgh33:${{ github.sha }}
